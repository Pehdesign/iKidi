@model IEnumerable<iKidi.Models.SubscriptionProductModel>
@using iKidi.Models;

@{
    ViewBag.Title = @Resource.Index;
}

<h2>@Resource.SubscriptionProduct</h2>

<p>
    @Html.Partial("_CreateButtonPartial")
</p>
<table class="table table-striped table-condensed">
    <tr class="success">
        <th>
            @Resource.Subscription            
        </th>
        <th>
            @Resource.Product
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SubscriptionTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductTitle)                
            </td>

            @Html.Partial("_TableButtonsPartial",
            new SmallButtonModel { SubscriptionId = item.SubscriptionId, ProductId = item.ProductId })
        </tr>
    }

</table>
